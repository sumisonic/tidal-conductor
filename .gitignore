# dependencies
node_modules/

# runtime artifacts
sessions/

# generated verification harness (regenerated by scripts, never committed)
ghci/

# training artifacts (raw.jsonl is the source of truth and is regenerated)
training/data/
training/adapters/
training/fused/
training/.venv/

# local secrets (.env is loaded by the pnpm scripts via scripts/env.mjs)
.env
.env.*
!.env.example

# nix / direnv
result
.direnv/

# macOS
.DS_Store
